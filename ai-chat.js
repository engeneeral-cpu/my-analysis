const messages=document.getElementById('messages'),input=document.getElementById('input'),form=document.getElementById('composer'),mic=document.getElementById('mic'),voiceStatus=document.getElementById('voiceStatus'),speakToggle=document.getElementById('speakToggle');
let history=[],voiceReplies=true,recognition=null;
function add(role,text,extra=''){const el=document.createElement('div');el.className=`bubble ${role} ${extra}`;el.innerHTML=`<strong>${role==='user'?'You':'Tara'}</strong><p></p>`;el.querySelector('p').textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight;return el}
function speak(text){if(!voiceReplies||!('speechSynthesis' in window))return;window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.rate=.98;u.pitch=1.02;u.volume=1;const lang=/[\u0C00-\u0C7F]/.test(text)?'te-IN':/[\u0900-\u097F]/.test(text)?'hi-IN':'en-IN';u.lang=lang;window.speechSynthesis.speak(u)}
async function send(text){text=text.trim();if(!text)return;add('user',text);history.push({role:'user',content:text});input.value='';input.style.height='auto';const typing=add('tara','Tara is thinking…','typing');try{const r=await fetch('/api/ai/chat',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({messages:history})});const data=await r.json();typing.remove();if(!r.ok||!data.success)throw new Error(data.error||'Tara AI is temporarily unavailable.');add('tara',data.reply);history.push({role:'assistant',content:data.reply});speak(data.reply)}catch(e){typing.remove();add('tara',e.message,'error')}}
form.addEventListener('submit',e=>{e.preventDefault();send(input.value)});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send(input.value)}});input.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,160)+'px'});speakToggle.addEventListener('click',()=>{voiceReplies=!voiceReplies;speakToggle.textContent=voiceReplies?'🔊 Voice replies: On':'🔇 Voice replies: Off';if(!voiceReplies)window.speechSynthesis?.cancel()});
const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(SR){recognition=new SR();recognition.lang='en-IN';recognition.interimResults=false;recognition.continuous=false;recognition.onstart=()=>{voiceStatus.textContent='Listening…';mic.textContent='⏹️'};recognition.onend=()=>{voiceStatus.textContent='Voice assistant ready';mic.textContent='🎙️'};recognition.onerror=()=>{voiceStatus.textContent='Could not hear that — try again';mic.textContent='🎙️'};recognition.onresult=e=>{const text=e.results[e.results.length-1][0].transcript;input.value=text;send(text)};mic.addEventListener('click',()=>{try{if(mic.textContent.includes('⏹'))recognition.stop();else{recognition.lang=/[\u0C00-\u0C7F]/.test(input.value)?'te-IN':'en-IN';recognition.start()}}catch{}})}else{mic.disabled=true;voiceStatus.textContent='Voice input is not supported in this browser'}

const $=id=>document.getElementById(id);const tabs=document.querySelectorAll('.tab');const modes={otp:$('otpMode'),password:$('passwordMode'),passkey:$('passkeyMode')};let timerId=null,seconds=30,verifiedPhone='';function setStatus(message,good=true){const el=$('status');if(!el)return;el.textContent=message||'';el.style.color=good?'#72d7a5':'#ff8f9a'}function digits(value){return String(value||'').replace(/\D/g,'').slice(0,10)}function switchMode(mode){Object.entries(modes).forEach(([key,el])=>el?.classList.toggle('hidden',key!==mode));tabs.forEach(tab=>{const active=tab.dataset.mode===mode;tab.classList.toggle('active',active);tab.setAttribute('aria-selected',String(active))});setStatus('')}tabs.forEach(tab=>tab.addEventListener('click',()=>switchMode(tab.dataset.mode)));$('phone')?.addEventListener('input',e=>e.target.value=digits(e.target.value));$('loginPhone')?.addEventListener('input',e=>e.target.value=digits(e.target.value));$('otp')?.addEventListener('input',e=>e.target.value=String(e.target.value||'').replace(/\D/g,'').slice(0,6));function startTimer(){clearInterval(timerId);seconds=30;$('timer').textContent='00:30';$('resendOtp').disabled=true;timerId=setInterval(()=>{seconds-=1;$('timer').textContent=`00:${String(Math.max(seconds,0)).padStart(2,'0')}`;if(seconds<=0){clearInterval(timerId);$('resendOtp').disabled=false;$('timer').textContent='Ready'}},1000)}async function sendOtp(){const phone=digits($('phone').value);if(!/^\d{10}$/.test(phone))return setStatus('Enter a valid 10-digit Indian mobile number.',false);$('sendOtp').disabled=true;setStatus('Sending secure OTP…');try{const r=await fetch('/api/auth/send-otp',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.success)throw new Error(d.error||'Unable to send OTP right now.');verifiedPhone=phone;$('otpArea').classList.remove('hidden');startTimer();$('otp').focus();setStatus('OTP sent successfully. Check your phone.')}catch(error){setStatus(error.message,false)}finally{$('sendOtp').disabled=false}}async function verifyOtp(){const otp=String($('otp').value||'').replace(/\D/g,'');if(!verifiedPhone)return setStatus('Request an OTP first.',false);if(!/^\d{6}$/.test(otp))return setStatus('Enter the 6-digit OTP.',false);$('verifyOtp').disabled=true;setStatus('Verifying OTP securely…');try{const r=await fetch('/api/auth/verify-otp',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone:verifiedPhone,code:otp})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.success)throw new Error(d.error||'OTP verification failed.');setStatus('Phone verified successfully. Opening Market Analysis…');setTimeout(()=>{window.location.href='tara.html'},350)}catch(error){setStatus(error.message,false)}finally{$('verifyOtp').disabled=false}}$('sendOtp')?.addEventListener('click',sendOtp);$('resendOtp')?.addEventListener('click',sendOtp);$('verifyOtp')?.addEventListener('click',verifyOtp);$('passwordLogin')?.addEventListener('click',()=>{const phone=digits($('loginPhone').value);if(!/^\d{10}$/.test(phone))return setStatus('Enter a valid 10-digit mobile number.',false);setStatus('Password login will be enabled after the secure account service is connected.')});$('passkeyLogin')?.addEventListener('click',()=>{if(!window.PublicKeyCredential||!navigator.credentials)return setStatus('Passkeys are not available in this browser. Use Mobile + OTP.',false);setStatus('Passkey support detected. Secure WebAuthn registration is the next authentication layer.')});$('forgotPassword')?.addEventListener('click',()=>{switchMode('otp');setStatus('Use mobile OTP to begin secure account recovery.')});switchMode('otp');